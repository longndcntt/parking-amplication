/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ParkingService } from "../parking.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { ParkingCreateInput } from "./ParkingCreateInput";
import { ParkingWhereInput } from "./ParkingWhereInput";
import { ParkingWhereUniqueInput } from "./ParkingWhereUniqueInput";
import { ParkingFindManyArgs } from "./ParkingFindManyArgs";
import { ParkingUpdateInput } from "./ParkingUpdateInput";
import { Parking } from "./Parking";
@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class ParkingControllerBase {
  constructor(
    protected readonly service: ParkingService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Parking",
    action: "create",
    possession: "any",
  })
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Parking })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async create(@common.Body() data: ParkingCreateInput): Promise<Parking> {
    return await this.service.create({
      data: data,
      select: {
        checkin: true,
        checkout: true,
        createdAt: true,
        id: true,
        plate: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Parking",
    action: "read",
    possession: "any",
  })
  @common.Get()
  @swagger.ApiOkResponse({ type: [Parking] })
  @swagger.ApiForbiddenResponse()
  @ApiNestedQuery(ParkingFindManyArgs)
  async findMany(@common.Req() request: Request): Promise<Parking[]> {
    const args = plainToClass(ParkingFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        checkin: true,
        checkout: true,
        createdAt: true,
        id: true,
        plate: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Parking",
    action: "read",
    possession: "own",
  })
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Parking })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async findOne(
    @common.Param() params: ParkingWhereUniqueInput
  ): Promise<Parking | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        checkin: true,
        checkout: true,
        createdAt: true,
        id: true,
        plate: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @nestAccessControl.UseRoles({
    resource: "Parking",
    action: "update",
    possession: "any",
  })
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Parking })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async update(
    @common.Param() params: ParkingWhereUniqueInput,
    @common.Body() data: ParkingUpdateInput
  ): Promise<Parking | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          checkin: true,
          checkout: true,
          createdAt: true,
          id: true,
          plate: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @nestAccessControl.UseRoles({
    resource: "Parking",
    action: "delete",
    possession: "any",
  })
  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Parking })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @swagger.ApiForbiddenResponse({ type: errors.ForbiddenException })
  async delete(
    @common.Param() params: ParkingWhereUniqueInput
  ): Promise<Parking | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          checkin: true,
          checkout: true,
          createdAt: true,
          id: true,
          plate: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
